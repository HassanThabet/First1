<analysis>
The previous AI engineer initiated work on the مدارس الفجر الجديد الأهلية application, focusing on critical PDF export fixes for Arabic (RTL) text. The primary challenge was integrating  and defining fonts to resolve  errors.

The current trajectory began with user confirmation to prioritize the PDF fix. The AI engineer systematically debugged by examining , , and relevant dashboard files. A key breakthrough was identifying that  directly exports the font data object (), but the calling code in , , and  incorrectly attempts to access it via . This structural mismatch was the root cause of the font loading failure, preventing  from finding the necessary font files. The immediate next step is to correct this direct assignment in the identified files.
</analysis>

<product_requirements>
The مدارس الفجر الجديد الأهلية application is a role-based dashboard system supporting Arabic (RTL) UI, offering CRUD operations for reports, graphical statistics, and Excel/PDF exports. Key users include Supervisors (daily activities), Vice-Principals (absent teachers, merged reports), Directors, and Chairmen (aggregated statistics, staff filtering, custom date ranges, charts). Recent updates involved enhancing Director and Chairman dashboards with departmental statistics and combined filters, along with a new branch filtering (boys/girls) requirement. The absent teachers responsibility shifted to Vice-Principal. A significant overhaul for PDF export formatting was initiated to achieve full RTL support and a unified Aklisha template. The immediate and current user request is to resolve the  error in PDF exports, followed by implementing automatic archiving of old reports. The purchases functionality was explicitly cancelled.
</product_requirements>

<key_technical_concepts>
-   **FastAPI:** Python framework for backend APIs.
-   **React:** Frontend library for UI.
-   **MongoDB:** NoSQL database.
-   **Shadcn UI & Tailwind CSS:** Frontend styling.
-   **RTL Support:** Essential for Arabic UI using .
-   **pdfmake & @digicole/pdfmake-rtl:** JavaScript libraries for client-side PDF generation.
</key_technical_concepts>

<code_architecture>
The application uses a MERN-like stack: React for the frontend, FastAPI for the backend, and MongoDB for the database.

**Directory Structure:**



**Key Files and Their Importance/Changes:**

-   :
    -   **Importance:** Director's dashboard with report display and PDF export.
    -   **Changes:** Updated to import  and . Its  function was refactored.  was defined.
-   :
    -   **Importance:** Chairman's dashboard.
    -   **Changes:** Similar to , updated to use  and . Its  function was refactored.  was defined. This file, along with  and , currently contains the incorrect  logic.
-   :
    -   **Importance:** Quality department's dashboard.
    -   **Changes:** Updated to import  and  was defined. This file also contains the incorrect  assignment.
-   :
    -   **Importance:** Centralized  configuration, especially for fonts and  setup.
    -   **Changes:** Imports  and defines . It correctly imports  from  and sets . However, this configuration is currently *not being used* by the affected dashboard files.
-    (New file):
    -   **Importance:** Centralizes PDF generation logic, styles, and RTL handling.
    -   **Changes:** Created to encapsulate PDF generation, imports , and includes helper functions. Contains its own  definition.
-   :
    -   **Importance:** Contains base64 encoded font data, specifically , for .
    -   **Changes:** No direct changes to its content. It  where  is the direct object containing font data.
</code_architecture>

<pending_tasks>
-   Address remaining PDF export formatting issues: Specifically, fix the  error.
-   Implement automatic archiving of old reports.
-   Implement remember me functionality for authentication.
-   Implement Chairman's detailed teacher evaluations and their PDF/Excel export.
</pending_tasks>

<current_work>
The immediate focus is on resolving the  error during PDF export. The previous AI engineer had integrated  and set  in various files.

The current AI engineer has confirmed with the user that fixing this PDF issue is the top priority. Diagnostic steps led to identifying the root cause:
1.  Confirmed 's presence in .
2.  Verified that  directly exports an object  containing the font data.
3.  Discovered that  correctly sets  (assuming  is the import from ).
4.  Crucially, found that , , and  were *not* using . Instead, they were directly configuring  and attempting to assign  by using an incorrect nested path: .

The work is at the stage where the precise structural mismatch has been identified: the code expects a nested  property, but the  export *is* the  object directly.
</current_work>

<optional_next_step>
Modify , , and  to correctly assign .
</optional_next_step>
